# 🎲 모둠뽑기 배포 안내서

## 파일 목록
- `index.html` → GitHub Pages에 올릴 앱 본체
- `Code.gs` → Google Apps Script에 붙여넣을 백엔드
- `배포안내서.md` → 이 파일

---

## STEP 1. GitHub Pages 배포 (index.html)

1. [github.com](https://github.com) 로그인
2. 오른쪽 상단 **+** → **New repository**
3. Repository name: `modum` (또는 원하는 이름)
4. **Public** 선택 → **Create repository**
5. **Add file** → **Create new file**
6. 파일 이름: `index.html`
7. `index.html` 내용 전체 붙여넣기
8. **Commit changes** 클릭
9. **Settings** 탭 → **Pages** → Source: **main branch** → **Save**
10. 잠시 후 `https://[내아이디].github.io/modum` 주소로 접속 가능!

---

## STEP 2. Google Apps Script 설정 (Code.gs)

1. [sheets.google.com](https://sheets.google.com) → 새 스프레드시트 만들기
2. 제목: `모둠뽑기 데이터` (원하는 이름)
3. 상단 메뉴: **확장 프로그램** → **Apps Script**
4. 기존 코드 전체 삭제 후 `Code.gs` 내용 붙여넣기
5. 💾 저장 (Ctrl+S)
6. 상단 **배포** → **새 배포**
7. ⚙️ 아이콘 클릭 → **웹 앱** 선택
8. 설정:
   - 설명: `모둠뽑기`
   - 다음 사용자로 실행: **나**
   - 액세스 권한: **모든 사용자**
9. **배포** 클릭 → Google 계정 권한 허용
10. 생성된 **웹앱 URL** 복사

---

## STEP 3. 앱에서 GAS 연결

1. 앱 접속 (GitHub Pages 주소)
2. **설정** 탭 → 하단 **연결 설정** 버튼 클릭
3. 복사한 GAS 웹앱 URL 붙여넣기
4. **저장하고 연결하기** 클릭
5. ✅ "Google 저장소 연결됨" 표시 확인

---

## 사용 방법

### 반 추가
- 상단 반 이름 클릭 → **반 선택/추가** 팝업
- 반 이름 입력 후 **추가**

### 모둠 배치
1. **설정** 탭에서 학생 이름 붙여넣기 (한 줄에 한 명)
2. 모둠 수 설정 (기본 6모둠)
3. 모둠별 인원수 개별 조정 가능
4. **X월 모둠을 뽑아주세요!** 클릭

### 고정 학생 설정
- 탭바 오른쪽 🔑 열쇠 버튼 → **고정 학생 설정**
- 시력 약한 학생 등 특정 모둠에 고정 가능
- 아이들 화면에 보이지 않음

### 결과 저장
- **결과** 탭 → **결과 저장하기**
- 저장된 기록은 다음 달 배치에 자동 반영

---

## 자주 묻는 질문

**Q. 다른 컴퓨터에서도 데이터가 유지되나요?**
→ GAS 연결 시 Google 스프레드시트에 저장되어 어디서든 동기화됩니다.
→ GAS 미연결 시 해당 브라우저에만 저장됩니다.

**Q. 3월에 자동 초기화된다는 게 뭔가요?**
→ 매년 3월 첫 접속 시 과거 기록이 초기화되어 새 학년 시작에 맞게 리셋됩니다.

**Q. 엑셀 파일을 바로 올릴 수 있나요?**
→ 엑셀에서 '다른 이름으로 저장 → CSV' 후 업로드하거나, 이름을 복사해서 붙여넣기 하세요.
